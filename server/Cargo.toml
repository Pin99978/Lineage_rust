[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { workspace = true }
crossbeam-channel = "0.5"
rusqlite = { version = "0.32", features = ["bundled"] }
serde_json = "1.0"
shared = { path = "../shared" }
pathfinding = "4.11"
